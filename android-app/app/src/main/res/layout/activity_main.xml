<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#101418">

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="MENIX SENTINEL"
        android:textColor="#FFCB05"
        android:textSize="28sp"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingBottom="8dp"/>

    <TextView
        android:id="@+id/alertBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp"
        android:textColor="#FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="center"
        android:visibility="gone"/>

    <!-- Capteurs -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#1c2128"
        android:padding="12dp"
        android:layout_marginTop="8dp">

        <TextView android:id="@+id/txtCh4"  style="@style/Sensor" android:text="CH₄ : --"/>
        <TextView android:id="@+id/txtCo"   style="@style/Sensor" android:text="CO  : --"/>
        <TextView android:id="@+id/txtO2"   style="@style/Sensor" android:text="O₂  : --"/>
        <TextView android:id="@+id/txtTemp" style="@style/Sensor" android:text="Temp: --"/>
        <TextView android:id="@+id/txtBat"  style="@style/Sensor" android:text="Bat : --"/>
    </LinearLayout>

    <TextView
        android:id="@+id/txtStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#888"
        android:textSize="12sp"
        android:paddingTop="6dp"
        android:gravity="end"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <!-- Boutons gros doigts (mineur avec gants) -->
    <Button android:id="@+id/btnTorch" style="@style/Big" android:text="TORCHE: OFF"/>
    <Button android:id="@+id/btnPhoto" style="@style/Big" android:text="PHOTO"/>
    <Button android:id="@+id/btnSos"   style="@style/BigDanger" android:text="SOS"/>
    <Button android:id="@+id/btnStop"  style="@style/Big" android:text="STOP"/>

</LinearLayout>
